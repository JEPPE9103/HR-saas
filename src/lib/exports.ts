export async function exportPDF(datasetId: string) {
  // Demo placeholder
  return { ok: true, url: `/exports/${datasetId}.pdf` };
}

export async function exportCSV(simResult: any) {
  // Demo placeholder
  return { ok: true, url: `/exports/adjustments.csv` };
}


